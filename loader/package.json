{
  "name": "aq-protocol-build",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js --minify",
    "watch": "node esbuild.config.js --watch"
  },
  "dependencies": {
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}